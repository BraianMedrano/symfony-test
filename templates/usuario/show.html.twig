{% extends 'base.html.twig' %}

{% block title %}Usuario
{% endblock %}

{% block body %}

	<div style="display: flex; justify-content: center;">

		<div class="container">
			<h1>Usuario</h1>

			<table class="table table-dark table-striped">
				<tbody>
					<tr>
						<th>Id</th>
						<td>{{ usuario.id }}</td>
					</tr>
					<tr>
						<th>Nombre</th>
						<td>{{ usuario.nombre }}</td>
					</tr>
					<tr>
						<th>Apellido</th>
						<td>{{ usuario.apellido }}</td>
					</tr>
					<tr>
						<th>Edad</th>
						<td>{{ usuario.edad }}</td>
					</tr>
				</tbody>
			</table>

			<div class="d-flex">
				<a class="btn btn-primary mr-3" href="{{ path('app_usuario_index') }}">back to list</a>

				<a class="btn btn-secondary mr-3" href="{{ path('app_usuario_edit', {'id': usuario.id}) }}">edit</a>

				{{ include('usuario/_delete_form.html.twig') }}
			<div/>


            
		</div>

	</div>

{% endblock %}
